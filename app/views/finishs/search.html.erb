

<p><%= render partial: "title_form" %></p>
<table class="table_design">
<tr>
 <%= render partial: "finish_column_title" %>
    </tr>
    
    <% a_price = 0 %>
<% @finishs.each do |finish| %> 
 <tr>
 <td><%= render partial: "finish", locals: {finish: finish} %></td>
 <%= render partial: "finish_list", locals: {finish: finish} %>
      

    </tr>
    <% a_price = a_price + finish.price if finish.price %>
    <% end %>
                <div style="foat: left;">
                <%= render partial: "finish_find_form" %>
              </div>

              <div>
              <% if current_user.id == (1 || 2 || 3) %>
              <div>合計:<%= number_to_currency(a_price,:precision =>0, :format=>"%n%u", :unit=>"円") %></div><!--precisionは小数点。標準だと小数点以下2位まで表示。%nはナンバー、%uがユニット。-->
                <% end %>
              </div>
              
</table>